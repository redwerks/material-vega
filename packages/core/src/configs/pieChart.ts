import { Config } from 'vega-embed';
import { MaterialVegaOptions } from '../options';

/**
 * Create config common to pie chart types
 */
export const createPieConfig = (options: MaterialVegaOptions): Config => {
  return {};
};
