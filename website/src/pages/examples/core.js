import React from 'react';
import Layout from '@theme/Layout';
import StorybookFrame from '../../ui/StorybookFrame';

const CoreExample = () => {
  return (
    <Layout title={`@material-vega/core examples`}>
      <StorybookFrame name="core" />
    </Layout>
  );
};

export default CoreExample;
